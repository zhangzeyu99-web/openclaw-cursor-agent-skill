# Task Prompt Structure

The canonical task prompt template is at `cursor-agent-system/templates/cursor-task-prompt.md`.

`spawn-cursor.sh` uses this template to auto-generate task files. Refer to the template for the full structure including placeholders for task name, description, project path, priority, and control instructions.
